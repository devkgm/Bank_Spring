<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		    
<mapper namespace="com.devkgm.app.product.ProductDAO">

	<sql id="search">
		WHERE
		<if test="kind==1">
			NAME LIKE '%'||#{search}||'%'
		</if>
		<if test="kind==2">
			ID LIKE '%'||#{search}||'%'
		</if>
		<if test="kind==3">
			RATE LIKE '%'||#{search}||'%'
		</if>
	</sql>
	
	<!-- <sql id="search">
		WHERE NAME LIKE '%'||#{search}||'%'
	</sql> -->
	<select id="getList" resultType="ProductDTO" parameterType="Pager">
		SELECT * FROM
		(
			SELECT ROWNUM R, RG.* FROM
				(
					SELECT *
					FROM PRODUCT
					<include refid="search"></include>
					ORDER BY ID DESC
				) RG
		)
		WHERE R BETWEEN #{startRow} AND #{endRow}
	</select>
	<select id="getDetail" resultType="ProductDTO" parameterType="ProductDTO">
		SELECT * FROM PRODUCT WHERE ID = #{id}
	</select>
	<select id="getTotalPage" resultType="Long" parameterType="Pager">
		SELECT COUNT(*) FROM PRODUCT
		<include refid="search"></include>
	</select>
	<insert id="add" parameterType="ProductDTO">
		<selectKey keyProperty="id" order="BEFORE" resultType="Long">
			SELECT PRODUCT_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO PRODUCT VALUES (#{id}, #{name}, #{description}, #{rate})
	</insert>
	<update id="update" parameterType="ProductDTO">
		UPDATE PRODUCT SET NAME=#{name}, DESCRIPTION=#{description}, RATE=#{rate} WHERE ID = #{id}
	</update>
	<delete id="delete" parameterType="ProductDTO">
		DELETE FROM PRODUCT WHERE ID = #{id}
	</delete>
	
	<insert id="addFile" parameterType="ProductFileDTO">
		<selectKey keyProperty="file_id" order="BEFORE" resultType="Long">
			SELECT FILE_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO FILES
		VALUES(#{file_id}, #{name}, #{origin_nm})
	</insert>
	
	<insert id="addProductFile" parameterType="ProductFileDTO">
		INSERT INTO PRODUCT_FILE
		VALUES(PRODUCT_FILE_SEQ.NEXTVAL, #{product_id}, #{file_id})
	</insert>
</mapper>